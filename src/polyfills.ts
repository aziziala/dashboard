/**
 * Zone JS is required by default for Angular itself.
 */
import '@angular/localize/init';
(window as any).global = window; // Polyfill for global in browser environments
import 'zone.js';  // Included with Angular CLI.
